# PrismVul — Python dependencies

# Core (numeric / ML baselines + shared utils)
numpy
scipy
scikit-learn
tqdm
colorama

# Data pipeline: scraping + HTTP
requests
aiohttp
beautifulsoup4
playwright

# LLM clients (CVE-description NLP stages + agentic baselines)
anthropic
openai

# Code parsing (C/C++ function extraction)
tree-sitter
tree-sitter-cpp

# Agentic baselines (zero-shot + in-context retrieval) — LLM agent over a git worktree
mini-swe-agent
litellm
jinja2

# Dense code-retrieval baseline (CodeRankEmbed)
torch
transformers
accelerate
